#Create a Vector class with x and a y attributes 
#Your vectors should handle vector additon with an .add() method that takes a second vector as an argument 
#and returns a new vector equal to the sum of the vector you call .add() on and the vector you pass in.


class Vector(object):
    def __init__(self,x,y):
        self.x = x
        self.y = y
    def add(self,b):
        return Vector(self.x +b.x,self.y + b.y)
