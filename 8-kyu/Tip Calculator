import math
def calculate_tip(amount, rating):
    
    return {'terrible': 0,'poor':math.ceil(amount*0.05),'good':math.ceil(amount*0.1),
            'great':math.ceil(amount*0.15),'excellent':math.ceil(amount*0.20)}.get(rating.lower(),'Rating not recognised')
